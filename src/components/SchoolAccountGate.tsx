/*
 * Blocking prompt when a student signs in with a personal Google account
 *
 * Kept mounted from App.tsx. Off-domain visitors are allowed to browse read-only
 * (banner on student pages). This overlay no longer blocks them.
 */

export function SchoolAccountGate() {
  return null
}
